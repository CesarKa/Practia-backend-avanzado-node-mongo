<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= siteTitle %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />

    <style>
      .header-link {
        color: #a855f7 !important;
        text-decoration: none;
      }
      .header-link:hover {
        color: #9333ea !important;
      }
      .btn-morado {
        background-color: #a855f7;
        color: white;
        border: none;
      }
      .btn-morado:hover {
        background-color: #9333ea;
        color: white;
      }
      .btn-outline-morado {
        color: #a855f7;
        border: 1px solid #a855f7;
      }
      .btn-outline-morado:hover {
        background-color: #a855f7;
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header
        class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom"
      >
        <div class="">
          <% getLocales().forEach( locale => { %>
          <a href="/change-locale/<%= locale %>" class="">
            <img
              width="50"
              height="50"
              src="/images/<%= locale %>.jpg"
              alt="<%= locale %>"
              class=""
            />
          </a>
          <% }) %>
        </div>
        <div class="col-md-3 mb-2 mb-md-0">
          <a href="/" class="d-inline-flex header-link fs-4">
            <%= siteTitle %>
          </a>
        </div>

        <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
          <% if (session.userId) { %>
          <li>
            <a href="/products/new" class="nav-link header-link px-2"
              ><%= __('New product') %></a
            >
          </li>
          <% } %>
        </ul>

        <div class="col-md-3 text-end">
          <% if (!session.userId) { %>
          <a href="/login" class="btn btn-outline-morado me-2">Login</a>
          <% } else { %>
          <small class="me-2 header-link"><%= session.userName %></small>
          <a href="/logout" class="btn btn-morado">Logout</a>
          <% } %>
        </div>
      </header>
    </div>
  </body>
</html>
